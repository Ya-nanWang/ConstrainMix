% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver.R
\name{constrain_mix}
\alias{constrain_mix}
\title{Generate Pseudo-Random Lists with Constraints}
\usage{
constrain_mix(
  data,
  constraints,
  n_lists = 1,
  max_steps = 5000,
  block_col = NULL,
  verbose = FALSE,
  show_feedback = TRUE,
  save_to = NULL,
  strict_mode = TRUE,
  track_occurrences = FALSE,
  track_by = NULL,
  occurrence_count_name = "Occurrence"
)
}
\arguments{
\item{data}{Data frame of items}

\item{constraints}{List of constraint objects}

\item{n_lists}{Number of lists to generate}

\item{max_steps}{Max optimization steps per block}

\item{block_col}{Optional block column name (if data already has blocks)}

\item{verbose}{Print detailed progress}

\item{show_feedback}{Show constraint feedback}

\item{save_to}{Directory to save CSVs (NULL = don't save)}

\item{strict_mode}{If TRUE, reject lists that violate ANY hard constraint (default TRUE)}

\item{track_occurrences}{If TRUE, add occurrence count column (default FALSE)}

\item{track_by}{Column to track occurrences (auto-detect if NULL)}

\item{occurrence_count_name}{Name for the occurrence count column}
}
\value{
List of results, each containing data, score, violations, etc.
}
\description{
Core low-level function: Generate pseudo-random lists that satisfy constraints.
Recommend using the higher-level generate_balanced_lists() function.
}
